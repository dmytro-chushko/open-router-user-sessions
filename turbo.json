{
  "$schema": "https://turborepo.dev/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"],
      "env": ["NEXT_PUBLIC_API_URL", "SESSION_COOKIE_NAME"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "env": ["NEXT_PUBLIC_API_URL", "SESSION_COOKIE_NAME"]
    },
    "start": {
      "cache": false,
      "persistent": true,
      "env": ["NEXT_PUBLIC_API_URL", "SESSION_COOKIE_NAME"]
    },
    "db:seed": {
      "cache": false,
      "env": [
        "DATABASE_URL",
        "ENABLE_ADMIN_SEED",
        "ADMIN_EMAIL",
        "ADMIN_SEED_PASSWORD"
      ],
      "inputs": [".env", ".env.*", "prisma/**"]
    }
  }
}
